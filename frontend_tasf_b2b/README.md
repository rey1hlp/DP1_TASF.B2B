# frontend_tasf_b2b

React + TypeScript app built with Vite for the Tasf.B2B simulation UI.

## Backend API

Set the backend URL with `VITE_API_BASE` (default: `http://localhost:8080`).

## Scripts

```bash
npm install
npm run dev
```

```bash
npm run build
npm run preview
```
